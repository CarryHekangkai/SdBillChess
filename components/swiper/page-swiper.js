// components/swiper/page-swiper.js
Component({
  /**
   * 组件的属性列表
   */
  properties: {
    banner: Object
  },

  /**
   * 组件的初始数据
   */
  data: {

  },

  /**
   * 组件的方法列表
   */
  methods: {
    bindBanner: function (event) {
      // let url = event.currentTarget.dataset.url;
      // wx.navigateTo({
      //   url: '/pages/web/web?url=' + url,
      // })
    }
  }
})
